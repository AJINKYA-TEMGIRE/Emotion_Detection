name: CI-Pipeline

on: push

68b22b6e60e1b64c1fab9273112d40c17e9b1e81
jobs:
  project-testing:
    runs-on: ubuntu:latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
         python-version: '3.10'

      - name: requirements.txt
        run: |
         pip install -r requirements.txt

      - name: Run pipeline
        env:
         CI: ${{ secrets.CI }}
        run: |
         dvc repro


